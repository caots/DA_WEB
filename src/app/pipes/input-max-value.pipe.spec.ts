import { InputMaxValuePipe } from './input-max-value.pipe';

describe('InputMaxValuePipe', () => {
  it('create an instance', () => {
    const pipe = new InputMaxValuePipe();
    expect(pipe).toBeTruthy();
  });
});

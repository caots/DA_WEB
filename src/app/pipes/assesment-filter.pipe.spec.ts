import { AssesmentFilterPipe } from './assesment-filter.pipe';

describe('AssesmentFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AssesmentFilterPipe();
    expect(pipe).toBeTruthy();
  });
});

import { IntegersOnlyDirective } from './integers-only.directive';

describe('IntegersOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new IntegersOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
